#include "../include/vec3.h"
Vec3 Vec3::operator*(double scaler) const 
{
  double scaled_x;
  double scaled_y;
  double scaled_z;
  Vec3 scaled_vector(scaled_x, scaled_y, scaled_z);
  return scaled_vector;
}
